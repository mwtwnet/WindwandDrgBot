import type { ButtonInteraction } from 'discord.js';
import type MyClient from '@utils/myClient';

export default {
    customId: 'open-apply',

    async execute(_interaction: ButtonInteraction, _client: MyClient) {
    },
};
